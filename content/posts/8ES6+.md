---
title: "ES6+ – Những tính năng JavaScript nên biết khi bắt đầu"
draft: false
tags: ["JavaScript"]
author: ["Trương Huy Phước"]
# Bắt đầu phần cấu hình ảnh bìa
cover:
    image: "/images/js-thumbnail1.jpg"  # Đường dẫn đến file ảnh
    alt: "Hình minh họa biến và kiểu dữ liệu trong JavaScript" # Văn bản mô tả ảnh (tốt cho SEO)
    caption: "Ảnh minh họa"            # Chú thích ảnh (tùy chọn)
    relative: false                    # Đặt false vì ảnh nằm trong thư mục static
---


### 1. Giới thiệu

JavaScript ngày xưa viết khá dài và dễ rối. Từ phiên bản ES6 trở đi, JavaScript được nâng cấp mạnh mẽ, giúp code ngắn hơn, dễ đọc hơn và ít lỗi hơn. Khi mới học, bạn không cần biết hết mọi thứ, chỉ cần nắm những tính năng dùng nhiều nhất là đủ.

### 2. Nội dung chính
### 2.1 let và const

Trước đây, JavaScript chỉ có var. ES6 giới thiệu let và const để code an toàn hơn.
```javascript
let tuoi = 10;
const ten = "An";
```

let → có thể thay đổi

const → không thay đổi

👉 Nên dùng let và const, hạn chế var.

### 2.2 Arrow Function (Hàm mũi tên)

Viết hàm ngắn gọn hơn.
```javascript
function tong(a, b) {
    return a + b;
}


Viết kiểu ES6:

const tong = (a, b) => a + b;
```

👉 Nhìn gọn và dễ đọc hơn.

### 2.3 Template String

Dùng dấu ` để nối chuỗi cho dễ.
```javascript
let ten = "An";
let tuoi = 10;

console.log(`Mình tên là ${ten}, ${tuoi} tuổi`);
```

👉 Không cần cộng chuỗi lằng nhằng nữa.

### 2.4 Destructuring (Tách dữ liệu)

Giúp lấy dữ liệu nhanh hơn.
```javascript
let hocSinh = {
    ten: "An",
    tuoi: 10
};

let { ten, tuoi } = hocSinh;
```

👉 Lấy dữ liệu gọn gàng.

### 2.5 Default Parameter

Cho giá trị mặc định nếu không truyền vào.
```javascript
function chao(ten = "bạn") {
    console.log("Xin chào " + ten);
}

chao();
```

👉 Tránh lỗi thiếu dữ liệu.

### 2.6 Spread Operator (...)

Giúp sao chép hoặc gộp dữ liệu.
```javascript
let so1 = [1, 2, 3];
let so2 = [...so1, 4, 5];

console.log(so2);
```

👉 Rất hay dùng với mảng và object.

### 2.7 for...of

Duyệt mảng dễ đọc hơn.
```javascript
let danhSach = ["An", "Bình", "Chi"];

for (let ten of danhSach) {
    console.log(ten);
}
```

👉 Không cần nhớ chỉ số.

### 2.8 Promise và Async / Await

ES6+ giúp xử lý bất đồng bộ dễ hiểu hơn.
```javascript
async function taiDuLieu() {
    await new Promise((resolve) => {
        setTimeout(resolve, 2000);
    });
    console.log("Tải xong");
}

taiDuLieu();
```

👉 Code giống như viết từng bước.

### 3. Những tính năng nên ưu tiên học trước

Nếu bạn mới học, hãy nhớ thứ tự này:

let / const

Arrow Function

Template String

Destructuring

Async / Await

👉 Chỉ cần vậy là đủ dùng.

### 4. Chia sẻ nhỏ khi học ES6+

Lúc mới học ES6, mình từng nghĩ phải nhớ hết mới giỏi. Nhưng thật ra, chỉ cần dùng thường xuyên, bạn sẽ quen rất nhanh. ES6+ giúp JavaScript dễ thở hơn rất nhiều cho người mới.

### 5. Tổng kết

ES6+ giúp code gọn và rõ ràng

Không cần học hết, chỉ cần học phần hay dùng

Gần như dự án JavaScript nào cũng dùng ES6+

Hiểu ES6+ là bạn đã sẵn sàng bước vào JavaScript hiện đại.